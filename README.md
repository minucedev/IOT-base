# IOT-base

## Kết nối SSH vào Raspberry Pi

```bash
ssh mypi@raspberrypi.local
```

- Host: `raspberrypi.local`
- Username: `mypi`
- Nếu không resolve được `.local`, dùng IP của Pi thay cho hostname.

